---
title: Carve Feature Demo
date: 2026-06-02
author: Carve
tags: [demo, reference]
---

%% This file exercises the Carve markup constructs supported by the grammar.
%% Lines beginning with %% are comments and do not render.

{#top}
# Carve Feature Demo

- dsfdsfsdf
- dfdsfds
+
```php
$foo = x;
```

A single document touching every core construct, plus a few renderer-dependent
extensions at the end. Open it with *Carve: Open Preview* to /see/ it rendered.

{#headings .featured}
## Headings and attributes

# Level 1
## Level 2
### Level 3
#### Level 4
##### Level 5
###### Level 6

A heading carries an explicit id and classes from a block-attribute line on
the line directly above it:

```carve
{#install .lead}
## Setup
```

*bold*

## Inline formatting

/italic/, *bold*, /*bold italic*/, _underline_, ~strikethrough~, =highlight=,
super{^script^} and sub{,script,}. Inline `code span`, and raw inline passthrough
`<abbr>HTML</abbr>`{=html} emitted verbatim.

An inline literal is verbatim but renders as prose, not code: the word cat is
!`/kaet/` in IPA, and an attributed one is !`/kaet/`{.ipa}.

Editorial markup: {+ added text +}, {- deleted text -},
{~ old phrase ~> new phrase ~}, and an {# editorial note #}.

Escapes keep specials literal: \*not bold\*, \/not italic\/, \#not-a-tag.

## Links and images

Inline [link](https://example.com), titled
[link](https://example.com "Hover title"), autolink <https://example.com>,
email autolink <hello@example.com>, and a reference [link][site].

A collapsed reference [site][] reuses the label as the reference.

Cross-reference to a heading by id: jump to </#top>.

[site]: https://example.com "Reference definition"

![A descriptive alt text](images/diagram.png)
^ Figure 1: images may carry a caption line.


## Lists

Unordered:

- apples
- oranges
  - clementines
  - blood oranges
- pears

Ordered:

1. clone the repo
2. install dependencies
   - run the build
   - run the tests
3. ship it

Tasks:

- [x] write the grammar
- [x] write the corpus
- [_] write more demos

Definition list:

:: Carve
:  A post-Markdown lightweight markup language.
ssdsd dsfdsfds
+
dsfdsfdsfdsf

:: Djot
:  The markup language Carve evolves from.

## Blockquotes

> The best way to predict the future is to invent it.
^ Alan Kay

## Tables

_italic_ dsfdsf

~xyy~ dsf dsf


|= Fruit       |=> Price |=~ Stock |
| Apple        |    1.20 |   In     |
| Banana       |    0.50 |   In     |
| Dragon fruit |    4.00 |   Out    |
^ Table 1: column alignment - left, right, center.

## Code and raw blocks

A fenced code block with a language:

```python
def greet(name: str) -> str:
    return f"Hello, {name}!"
```

A quoted "header" after the language sets the block's title (rendered as the
`title` attribute on the `<pre>` - a filename bar or hover tooltip):

```python "greet.py"
def greet(name: str) -> str:
    return f"Hello, {name}!"
```

A tilde fence works too:

~~~yaml
key: value
list:
  - one
  - two
~~~

A raw block passes content through to a named format unchanged:

```=html
<details><summary>Native HTML</summary>Rendered as-is.</details>
```

## Math

Inline math uses a dollar before a code span: $`e = mc^2`.

Display math uses a double dollar:

$$`e^{i\pi} + 1 = 0`

A fenced `math` block is the GFM-style block form (mathBlock extension):

```math
\sum_{k=1}^{n} k = \frac{n(n+1)}{2}
```

## Admonitions and divs

::: note
A plain note callout.
:::

::: tip "With a title"
Admonitions accept an optional quoted title.
:::

::: warning
Heed this warning.
:::

::: danger
Critical information.
:::

A bare `:::` fence with no admonition type is a generic styled container, not a
colored callout. Its id and classes go on a block-attribute line directly above
the fence:

{#callout .highlight}
:::
This block has only an id and classes, so it renders as a plain
`<div id="callout" class="highlight">` wrapper you can target with your own CSS.
:::

## Mentions, tags, and inline extensions

Mention a person with @alice and reference a tag like #release-1.0.
An inline extension names a role: press :kbd[Ctrl+C] to copy.

## Footnotes

Carve supports footnotes[^note] with definitions collected anywhere.[^second]

[^note]: The reference and its definition are linked by label.
[^second]: Definitions can appear in any order.

## Smart typography

Dashes and ellipsis: an em dash --- an en range 10--20 ... and so on.
Arrows and comparisons: -> <- <-> => , != <= >= , (c) (r) (tm).

---

## List tables (Tier-3 extension)

%% A `::: list-table` block authors a table as a nested list, so cells can hold
%% full block content a native pipe table cannot. It only renders as a real
%% <table> when the renderer enables the listTable extension; otherwise it
%% degrades to a `<div class="list-table">` holding the literal list.

%% Attributes sit on the PRECEDING line: header-rows=N promotes the first N rows
%% to <thead>. A cell whose sole content is a lone `^` spans the row above; a
%% lone `<` spans the cell to the left - the same markers native pipe tables use.

{header-rows=1}
::: list-table "Quarterly results"
- - Region
  - Notes
- - EMEA
  - Strong quarter.

    Drivers:

    - new logos
    - renewals
- - APAC
  - <
:::

## Code groups (Tier-3 extension)

%% `::: code-group` turns several fenced blocks - each with a `[label]` tab - into
%% a tabbed widget (codeGroup extension). Without it, it degrades to a plain div
%% of sequential code blocks.

::: code-group
```bash [npm]
npm install @markup-carve/carve
```

```bash [pnpm]
pnpm add @markup-carve/carve
```

```bash [yarn]
yarn add @markup-carve/carve
```
:::

## Mermaid (Tier-3 extension)

%% Mermaid is a third-party extension, not core Carve. It is written as a
%% fenced code block tagged `mermaid`. It only renders as a diagram when the
%% renderer enables the Mermaid extension; otherwise it shows as a code block.

```mermaid
graph TD
    A[Write Carve] --> B{LSP diagnostics}
    B -->|clean| C[Preview]
    B -->|warnings| D[Quick fix]
    D --> A
    C --> E[Publish]
```

```mermaid
sequenceDiagram
    Author->>Editor: type Carve
    Editor->>LSP: textDocument/didChange
    LSP-->>Editor: diagnostics + semantic tokens
    Editor-->>Author: live preview
```

## Charts (Tier-3 extension)

%% A fenced `chart` block carries a Chart.js config as JSON. It renders as a live
%% chart when the renderer enables the chart preset (and Chart.js is loaded);
%% otherwise it degrades to an inert `<div class="chart">` hydration marker.

```chart
{"type":"bar","data":{"labels":["Q1","Q2","Q3","Q4"],"datasets":[{"label":"Revenue","data":[12,19,14,23]}]}}
```

## Spoilers (Tier-3 extension)

%% Inline :spoiler[…] blurs until you click it; a ::: spoiler block collapses to
%% just its summary and expands on click. Without the extension they degrade to a
%% readable span / div so the document still reads.

The killer is :spoiler[the lighthouse keeper] - click the blur to reveal.

::: spoiler "Plot ending"
Everyone makes it home. The dog lives too.
:::
